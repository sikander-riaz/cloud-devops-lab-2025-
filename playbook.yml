- hosts: all
  gather_facts: no
  become: yes

  tasks:
    - name: Ping machine
      ping:

    - name: "Ubuntu Update"
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: "Install Docker"
      apt:
        name: ["nginx"]
        state: latest
