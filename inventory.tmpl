
[bastion]
${bastion_ip} ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/${ssh_key}.pem

[app]
${app_ip} ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/${ssh_key}.pem ansible_ssh_common_args='-o ProxyCommand="ssh -W %h:%p -q ec2-user@${bastion_ip} -i ~/.ssh/${ssh_key}.pem"'
